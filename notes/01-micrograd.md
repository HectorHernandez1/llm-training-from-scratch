# 01 — micrograd

> Maps to: Video 1 — The spelled-out intro to neural networks and backpropagation:
> building micrograd. Repo: https://github.com/karpathy/micrograd

**▶ Resume at:** Video 1 @ 1:39:33 (paused 2026-08-02) — automatic `_backward` closures for all ops + topological-sort `backward()` done and verified against the manual grads (`x1.grad=-1.5`, `w1.grad=1.0`). Next up: break `tanh` into granular ops (`exp`/sub/div) to show the engine is op-agnostic, then build `Neuron`/`Layer`/`MLP` + the training loop. Type-along notebook: [`week1-micrograd/micrograd.ipynb`](../week1-micrograd/micrograd.ipynb).

## Key concepts

## Code I wrote

## Questions I had

## Things that surprised me
